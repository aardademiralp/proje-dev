# ISU-SecOps-Orchestrator Bağımlılıkları
# ========================================
# Bu dosya Python/FastAPI orkestratör katmanı için tüm bağımlılıkları tanımlar.
#
# Kurulum:
#   pip install -r requirements.txt
#
# Geliştirme bağımlılıklarıyla:
#   pip install -r requirements.txt

# ---------------------------------------------------------------------------
# Çekirdek Web Framework
# ---------------------------------------------------------------------------
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.30.0,<1.0.0

# ---------------------------------------------------------------------------
# Veri Doğrulama ve Konfigürasyon
# ---------------------------------------------------------------------------
pydantic>=2.7.0,<3.0.0
pydantic-settings>=2.3.0,<3.0.0

# ---------------------------------------------------------------------------
# HTTP İstemcisi (Test ve Webhook için)
# ---------------------------------------------------------------------------
httpx>=0.27.0,<1.0.0

# ---------------------------------------------------------------------------
# Test Altyapısı
# ---------------------------------------------------------------------------
pytest>=8.2.0,<9.0.0
pytest-asyncio>=0.23.0,<1.0.0
pytest-cov>=5.0.0,<7.0.0
pytest-mock>=3.14.0,<4.0.0

# ---------------------------------------------------------------------------
# Geliştirme Araçları (opsiyonel ama önerilir)
# ---------------------------------------------------------------------------
# ruff>=0.4.0          # Hızlı Python linter ve formatter
# mypy>=1.10.0         # Statik tip denetleyici
# pre-commit>=3.7.0    # Git hook yöneticisi
